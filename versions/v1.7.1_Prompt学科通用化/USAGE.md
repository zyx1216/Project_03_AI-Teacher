# AI教学辅助 · 使用说明（v1.7.1）

本手册面向第一次使用的老师。软件在你自己电脑上运行，数据不联网、不上云（除了你主动调用的 AI 模型）。


## 零、v1.7.1 Prompt 学科通用化

### 1. AI 出题适配所有学科
- AI 出题会根据任务中的学科、年级、知识点、题型、难度和数量命题，不再按数学单一学科理解请求。
- 小学题符合对应年龄段认知，初中题对标中考要求，高中题对标高考要求，减少跨学段超纲。
- 理科题目可以使用公式；语文、英语、政治、历史、地理等文科题目按正常文字出题，不强行套公式。
- 只有数学、物理、化学等有明确算式且能自动验算的题，系统才会使用 `verify` 字段。

### 2. AI 备课适配所有课堂
- AI 备课会根据学科、课题、学情和参考资料生成教案，不再把教案限定为数学教案。
- 新授环节会按学科需要安排“例题 / 范文 / 实验演示”。
- 小学教案更强调活动和趣味性，初中教案强调衔接和方法，高中教案强调深度和思维训练。

### 3. 章节和教材识别变化
- “数学好玩”属于数学教材特定栏目，不再进入跨学科通用章节规则。
- 高中资料标题支持“选择性必修、必修一、必修二、必修三”的年级识别；“选择性必修”按高三口径处理。

## 零、v1.7.0 章节单选与暂存上下文保留

### 1. 章节只选一个
- 在 AI 出题页选择资料后，通过“📖 参考章节（来自资料，单选）”下拉框选择一个章节。
- 不需要章节时保持“不指定章节”。
- 知识点仍是多选；题库为空时，可在“手动补充知识点”里输入新知识点。

### 2. 暂存后不用重复选择资料
- 点击“📌 暂存当前配置”后，题型行、手动知识点和其他要求会清空。
- 已选资料、章节和知识点会保留；成功提示会显示本次章节，方便核对。
- 要配置下一个章节，直接手动切换章节即可。

### 3. 直接入栏同样保留上下文
- 点击“➕ 添加当前配置为任务”后，任务进入任务栏，资料、章节和知识点继续保留。
- 切换学科或资料时，章节和知识点会清空，避免误用旧上下文。

## 零、v1.6.9 待定任务暂存机制

### 1. 先暂存一组配置
- 在 AI 出题页选择年级、学科、资料、章节、知识点，并在题型表格中填写一组多行题型。
- 点击“📌 暂存当前配置”，系统会把这一整组保存到“待定任务草稿箱”。
- 暂存后，当前章节、知识点、其他要求和题型表会清空，可以继续配置下一组。

### 2. 管理草稿组
- “📥 待定任务草稿箱”会逐组显示学科、年级、章节、知识点和题型数量。
- 某一组配置不要了，点该组旁边的“🗑️ 删除该组”。
- 配置完多组后，点击“✅ 全部加入任务栏”：每组里的每一行题型会展开成一条任务。

### 3. 继续使用任务栏生成
- 草稿展开后进入原有“📋 出题任务栏”，再按勾选状态点击“🤖 生成勾选任务的题目”。
- 如果不需要分组，也可以继续点击“➕ 添加当前配置为任务”直接入栏。

## 零、v1.6.8 出题 Bug 修复与题库年级筛选

### 1. AI 出题数量不会丢
- 在“题型、难度、数量”表格里改好数量后，点击“➕ 添加一行”，原来几行的数量会保留，不会被重置。
- 一次填写多行后点击“➕ 添加当前配置为任务”，每行都会生成一条任务，不会只加入第一条。
- 多条任务进入下方“📋 出题任务栏”后，仍可按勾选状态删除、清空或生成。

### 2. 题库按年级筛选
- 进入“题库管理”，在筛选区的“年级”下拉框选择具体年级，例如“三年级”或“初一”。
- 选择具体年级后，只显示该年级题目；切回“全部年级”会显示所有题目。
- 以前保存的历史题目没有年级标记，不会出现在具体年级中，但会在“全部年级”里显示。

### 3. 新题目的年级来源
- 备课区“AI 出题”生成并确认入库的题目，会自动带上任务栏里的年级。
- 作业页 AI 生成、手工录入、Word/Excel 导入的题目本版不自动写年级。

## 零、v1.6.7 导出优化、勾选导出、上传清空

### 1. 题库勾选后再导出
- 题库管理页的题目表格支持勾选：勾选要导出的题目后，再点表格下方的导出按钮。
- 按钮文案会实时显示已选数量，例如“导出所选习题卷（仅题目，已选 3 题）”。
- 没有勾选时点击只会提示“请先在表格中勾选要导出的题目”，不会生成文件。
- 导出顺序按你勾选的顺序，题号从 1 重新连续编号。

### 2. 学生卷和教师卷的区别
- 学生卷（仅题目）：标题下有“班级、姓名、得分”填写行；不显示题型、难度和知识点。
- 教师卷（含答案解析）：保留题型、难度、知识点、答案、解析和易错点。
- 选择题的四个选项会各自成行并缩进；导出时会清理 `# AI教学辅助 · 使用说明（v1.6.7）

本手册面向第一次使用的老师。软件在你自己电脑上运行，数据不联网、不上云（除了你主动调用的 AI 模型）。


、`\(`、`\)` 等公式边界标记，公式按普通文字显示。

### 3. 资料上传框自动清空
- 资料真正保存成功后，文件选择框会自动清空，方便继续上传下一份。
- 还在命名确认或章节预览阶段时，已选文件会保留。
## 零、v1.6.6 PDF 目录识别优化

### 1. 导入 PDF 后的识别顺序
- 上传 PDF 并确认资料名称、年级后，系统先读取目录页，用正则提取章节标题和页码。
- 目录能提取到至少 3 个章节时直接使用，不调用 AI；提取不足时才自动使用 AI。
- 识别成功后，页面会显示来源和偏移量，例如“目录正则提取、页码偏移 2 页”。

### 2. 调整页码偏移
- PDF 前面常有封面、扉页，导致 PDF 页码和书本印刷页码不同。
- 在“页码偏移说明”区域修改“页码偏移量”，系统会按 `PDF页码 = 印刷页码 + 偏移量` 重算全部章节。
- 点击“重新自动检测”可重新识别；整体重算会覆盖此前手动修改的单个章节页码。

### 3. 书签和手工编辑
- 如果 PDF 有内置书签，可点击“使用 PDF 书签”；书签页码按 PDF 页码处理，默认偏移为 0。
- 下方章节表仍可修改标题、层级和页码，空标题保存时自动跳过。
- 目录和 AI 都识别失败时，系统按正文标题规则切分，不影响保存资料。

## 零、v1.6.5 教案模板通用化和出题任务栏

### 1. 教案模板
- AI 备课的内置教案模板只保留“通用模板”，结构包含教学目标、重难点、教学准备、完整教学过程、板书设计和教学反思。
- 以前保存过的“语文模板/数学模板”不会再出现在下拉框；自己保存的自定义模板继续保留。
- “通用模板”是内置名称，不能用自定义模板覆盖。

### 2. 添加出题任务
- AI 出题页顶部的年级和学科只用于设置新任务默认值。
- 选择资料和知识点后，在“题型、难度、数量”动态表格中逐行填写；每个非空行会生成一条独立任务。
- 知识点候选不足时，可在“手动补充知识点”中填写，多个知识点用逗号或顿号分开。
- 点击“➕ 添加当前配置为任务”后，任务进入下方“📋 出题任务栏”；当前题型表、资料、知识点和补充要求会清空，顶部年级和学科保留。

### 3. 管理和生成任务
- 在任务栏中勾选要处理的任务，可使用“🗑️ 删除勾选任务”或“🧹 清空所有任务”。
- 点击“🤖 生成勾选任务的题目”后，系统逐条任务生成；页面会显示当前进度。
- 混合学科或混合年级任务会按每条任务自己的参数请求模型，某一条失败不会中断其他任务。
- 生成结果按学科分组预览，同学科不同年级会在题目信息中显示年级；确认后统一入库，缺答案题继续拒收。

### 4. 旧草稿迁移和历史恢复
- 如果旧版 `question_drafts.json` 有待定内容，第一次进入 AI 出题页会自动迁移到任务栏，随后清空旧草稿。
- 出题历史中的“带回配置”会把任务写回任务栏，不会自动调用模型；检查配置后再点击生成。

## 零、v1.6.4 AI 备课区优化

### 1. AI 备课章节选择
- 选择资料后，在资料选择器下方的“选择章节（可搜索）”中点开下拉框，直接输入关键词即可过滤章节。
- 页面只保留这一套章节选择；勾选的章节就是生成教案时使用的资料范围。

### 2. 备课参数和模板
- “📝 备课参数”区域依次填写课题、课时数、课堂风格和教案模板。
- 自定义教案模板仍可通过模板下拉框导入，导入 Word 或粘贴正文都保留。

### 3. PDF 章节识别
- PDF 如果有内置书签，建议优先点击“📑 使用 PDF 书签”，章节通常更准确。
- 遇到“第一单元”单独一行、下一行是单元名称的教材，系统会自动合并成“第一单元 单元名称”。
- “整理与复习、总复习、综合实践、练一练”会作为通用固定标题识别；数学专属栏目“数学好玩”自 v1.7.1 起不再进入通用规则。无编号的普通短行不会强行切分，避免把正文切错。

## 零（旧）、v1.6.3 备课区全面优化

### 1. 资料保存后自动建索引
- 保存资料后系统在后台自动建立检索索引，完成后状态变为“已索引”，无需手动点击。
- 建立索引失败不会影响已保存资料，可点“建立索引”重试。

### 2. 资料详情和删除
- 资料详情全文改为可滚动大文本框，能看完整内容；点章节按钮只看该章节。
- 每项资料有“🗑️ 删除”，二次确认后会连同文本、原版文件和索引一起删除。

### 3. 年级名称
- 初中年级统一显示为 初一/初二/初三（数据库内部仍按七/八/九年级保存），并新增 高一/高二/高三。
- 选择“初二”等显示名时，历史保存为“八年级”的资料仍能正常匹配。

### 4. 教案草稿
- 教案一生成即自动保存为“课题（草稿）”；编辑后点“💾 保存教案”更新同一条，标题可自行修改。
- 同一课题重复生成会复用已有草稿，不会重复建记录。

### 5. 出题待定任务
- 各学科待定任务会保存到本地，切换页面或重开软件后仍在；“🧹 清除所有待定任务”可一键清空。
- 任务表清空难度/数量单元格不再报错。出题历史可分页，并能“查看题目”。

### 6. 题库表格与 PPT
- 题库题目改成表格：点行看详情，勾选多行后在上方“✅ 批量审核通过”。
- 知识点组卷的知识点可从题库下拉多选，也能手动补充。
- PPT 生成后先预览每页内容，确认再下载；自定义模板支持改名和删除。

## 零（旧）、v1.6.2 备课区体验优化

### 1. 打开或下载资料原版
- 新导入的 PDF/Word 会保存原始文件。资料列表中，PDF 在浏览器新标签页打开；Word 直接下载；网络资料打开原网页。
- “详情”按钮继续进入页内详情，可看内容预览和章节列表。旧资料如果没有原文件，只显示详情入口。
- 扫描件 OCR 的原文会先暂存；确认资料名称后自动转到原文目录。删除任务记录时，暂存原文和识别结果会一起清理。

### 2. AI 备课章节选择
- 选择资料后，在“章节搜索与选择”边框区域输入关键词，再勾选章节。
- 更换关键词不会清空已选章节；生成教案时只使用勾选章节。

### 3. AI 出题学科切换
- 学科改成横向单选。切换学科时，当前学科的任务、资料、知识点和补充要求会自动暂存。
- 任务表勾选“删除”后点击“🗑️ 删除勾选任务”，不会影响其他学科。
- 点击生成时，系统会汇总所有有待定任务的学科，并按学科分组预览。

### 4. PDF 章节识别
- PDF 预览区可先点“📑 使用 PDF 书签”；没有书签时，可点“🤖 AI 识别章节”。
- 识别后可直接修改章节标题、层级和页码；空标题保存时自动跳过。
- AI 识别失败时仍可用规则切分，不影响资料保存。

## 旧版备课区功能补充（v1.6.0）

### 1. 资料详情与导入命名
- 在「📚 备课 → 资料管理」点击资料名称即可进入详情页，查看学科、年级、上传时间、内容预览和章节列表。
- 上传 PDF/Word、粘贴文本或导入公开网页后，系统先提取内容，再弹出“确认资料信息”窗口；在这里修改资料名和年级，确认后才会保存。
- 资料列表上方可按年级、学科同时筛选；网页来源显示为“网络导入”。

### 2. OCR 停止与命名
- 扫描件 PDF 会进入后台 OCR，识别中显示“正在识别第 X / N 页...”。
- 可点击“停止识别”，系统会在当前页结束后停止；失败或已停止的任务可点击“清除进度”。
- OCR 完成后点击“确认命名”，核对资料名称和年级后才创建资料；取消则不创建。

### 3. AI 备课章节和模板
- 选择资料后，用章节关键词搜索并勾选章节；生成教案时只把选中章节作为上下文。
- 可使用通用、语文、数学模板，也可以粘贴文本或导入 Word 保存自定义模板。

### 4. AI 出题、题库和 PPT
- AI 出题支持多个学科，逐学科生成后统一预览；多任务表格可添加不同题型、难度和数量。
- 出题历史保留最近 50 条，可把历史配置带回页面，但不会自动调用模型。
- 题库管理可按知识点和难度组卷；只有全部规则满足时才创建试卷。
- 历年真题可通过 Word、文字版 PDF、扫描件 PDF 导入；扫描件先完成后台 OCR。
- PPT 支持简约、教育、商务主题，也可上传自定义 PPTX 模板。

## 一、启动与退出

### 1. 启动（命令行方式）

打开 Anaconda Prompt（或 PowerShell），执行：

```powershell
conda activate math_teacher
cd D:\Codex\Project_03_AI数学教师工作台
streamlit run app.py
```

启动后浏览器会自动打开 <http://localhost:8501>。没自动打开就手动在浏览器地址栏输入这个网址。

### 2. 在 PyCharm 里启动（推荐配置一次，以后点绿色三角）

1. 右上角「运行配置」→「编辑配置」→ 点 **+** → 选 **Python**。
2. 「运行于」选模块名方式：
   - 模块名填：`streamlit`
   - 参数填：`run app.py`
3. Python 解释器选 conda 环境 `math_teacher`（Python 3.11）。
4. 工作目录设为项目根目录 `D:\Codex\Project_03_AI数学教师工作台`。

> **常见错误**：不要直接点 app.py 的「运行」。那样会出现一堆 `missing ScriptRunContext` 警告，页面功能不正常。**必须用 `streamlit run app.py`。**

### 3. 退出

在运行 Streamlit 的终端窗口按 `Ctrl + C`，然后关闭浏览器标签页即可。数据会自动保存，不用手动存。

---

## 二、首页与快捷入口

打开软件默认进入「🏠 首页」。首页直接读取本机数据库：

- 4 个卡片：本学期备课次数、本月布置作业次数、最近考试平均分、待批改作业数；没有数据时显示 `—`。
- 最近动态：最近 3 场考试（含均分变化）、最近 3 次作业、待批改清单。
- 本周日历预览：查看本周 7 天的考试、作业和教案；点击按钮可进入完整教学日历。
- 本周课程表：切换班级查看每周重复课程，也可进入完整日历维护。
- 快捷入口：
  - **新建作业**：进入作业页并打开新建作业弹窗。
  - **上传成绩**：进入学情页「成绩管理」并展开成绩导入区；空库时会默认选到“新建考试”。
  - **AI 备课**：进入备课页「AI 备课」Tab。

这些跳转只改变本应用内的受控状态，不会写入或修改业务数据。

---

## 三、第一次使用（5 分钟配好 AI）

除纯统计、导出等功能外，AI 备课、出题、反思、评语都需要先配模型。

进入左侧「⚙️ 设置」，从上到下三组配置：

| 配置 | 用途 | 建议 |
| --- | --- | --- |
| **主模型** | 考试分析、学情分析、教学反思、期末评语、作业 AI 总结 | DeepSeek `deepseek-chat`，或通义/豆包 |
| **内容生成模型** | AI 教案、AI 出题、自动组卷补缺 | 可与主模型相同；留空则自动用主模型 |
| **向量模型 Embedding** | 上传课本后的语义检索 | 火山方舟 embedding；**不配也能用**，会自动降级为关键词检索 |

填写步骤（以 DeepSeek 为例）：

1. 主模型区：粘贴 API Key → Base URL 填 `https://api.deepseek.com` → 模型填 `deepseek-chat`。
2. 点「💾 保存配置」，再点「🔌 测试连接」，看到“连接成功”即可。
3. 内容模型区留空（自动用主模型），或单独填一个更强的模型。
4. 向量模型可先不配，需要课本语义检索时再回来填。

**Key 存在哪里**：API Key 通过 `keyring` 存在 Windows 凭据管理器（服务名 `math-ai-teacher`），不写进任何项目文件，不会被 git 提交或打进备份包。Base URL、模型名等非敏感配置存在 `data/llm_config.json`。

> 换电脑迁移时：恢复备份 zip 后，Key 需要在新电脑重新填一次（备份不含 Key）。

---

## 快捷键（全局生效）
- `Ctrl+1 / 2 / 3 / 4`：切换 首页 / 教学日历 / 备课 / 作业。
- `Ctrl+K`：聚焦当前页第一个搜索框。
- 侧边栏「⌨️ 快捷键」折叠区也有同样说明；`Ctrl+N`、`Ctrl+S` 是浏览器保留键，本应用未绑定。

## 四、📊 学情

顶部 8 个标签页。

### 1. 学生管理

- **Excel 批量导入**：准备一个 Excel，第一行表头含「姓名」（必填），可选「学号/班级/性别/备注」。表头名字写成“名字、学生姓名、name”也能识别。上传后先看**预览和字段映射**，缺姓名的行会标红，确认无误点导入。
- **手动添加**：填表单个新增。
- **直接编辑表格 / 新增**：下方表格可直接修改六列（姓名、学号、班级、性别、标签、备注），表格最后一行可直接填新学生；点“💾 保存表格修改”后统一写入。性别只填“男/女”，其它值会按未设置处理。保存后按“班级 → 学号”自动排序归位。
- **筛选**：支持按班级、姓名/学号关键词、性别（含“未设置”）和标签筛选；标签直接在“标签”列用逗号或顿号分隔书写（如“积极,计算薄弱”）。筛选只影响表格里显示的学生，保存时只同步当前筛选结果，避免误处理不可见学生。
- **删除学生**：在表格里删除行后再保存，会先提示将级联删除成绩和评语，确认后才真正删除。
- **批量删除**：勾选表格最前“选择”列的多名学生，点“🗑️ 批量删除所选”，二次确认后一次删除。
- **查看画像**：表格上方“查看学生画像”选一个学生，点“🔍 查看画像”可直接跳到画像标签页。
- 学生按「姓名 + 班级」判重，重复导入是更新不是新建。

### 2. 成绩管理

- **下载模板**：导入区点 **⬇️ 下载 Excel 模板**，模板含姓名、学号、班级和 9 科列，第二行是示例数据；删掉示例行、填好真实成绩即可导入，未填的科目按缺考处理，不会写入 0 分。
- **Excel / Word / PDF 导入成绩**：Excel 里一列姓名，其余每列是一门科目分数（列名如「数学」「数学成绩」都可识别）；Word 需使用真表格；PDF 仅支持有清晰线框的规范表格。上传后进入统一可编辑预览，可直接修改姓名、班级、分数，缺分数格按缺考处理（不计入统计）。
- 导入时可以二选一：**选择已有考试**，或直接切到 **新建考试**。新建时填写考试名称、日期、年级和学期，系统识别到科目后可逐科调整满分（语文、数学、英语默认 150，其余默认 100）；确认导入时会先建考试再写成绩。同名同日期考试不能重复创建，同名不同日期可以。
- 如果 PDF 是扫描件、无线框或版式复杂，系统会提示复制到 Excel 后导入，本版不做 OCR。
- **手动录入/修正**：直接在表格里改单个分数。
- 可导出**格式化成绩单 Excel**：含标题、姓名、学号、班级、各科成绩、总分、班级排名，末尾附平均分、最高分和最低分。
- “历次考试成绩”折叠区提供跨考试长表，一行是一名学生的一场考试的一科，可按班级、学期、学年、考试类型筛选后导出。
- 也可以先在「🗂️ 考试管理」里点 **➕ 新建考试**：各科满分用逐行表格填写，空行会忽略，同学科不能重复，满分必须大于 0。

### 3. 考试分析

先用“学期”筛选缩小考试范围（学期为空的旧考试只在“全部学期”显示），再选一场考试（可再选某个班）：

- 指标卡：参考人数、平均分、最高分、最低分、及格率、优秀率。
- 分数段图按“未及格 / 及格 / 优秀”三段展示，可切换柱状图或饼图；各科均分图可切换柱状图或折线图；学生排名用横向条形图。
- 排名表和排名条形图都可选择高分到低分或低分到高分；并列同名次规则不变（如两人并列第 1，下一个就是第 3），缺考不参与。
- **进退步**：与按日期排序的上一场考试对比，进步标绿、退步标红。
- 点「🤖 生成考试分析」可得到 ≤300 字的整体→亮点→问题→建议（需配主模型）。
- **导出学期报告**：先在上方选择具体学期和班级，再点「⬇️ 导出学期报告（Word）」；报告含本学期考试汇总、各科统计、成绩趋势图、高频错题 TOP5 和学生成绩变化。全部学期下不生成。
- 可导出 Word 报告。

及格线、优秀线默认分别为满分的 60%、85%；可在本页最顶部的「⚙️ 考试分析线」折叠区调整（空库也能打开），优秀线必须高于及格线。

### 4. 趋势分析

- 顶部两个独立下拉：**时间筛选**（全部 / 某个学期或学年）和**显示范围**（最近 3/5/10 次、全部，默认最近 5 次）。先按时间筛选过滤，再取最后 N 场，过滤后不足 N 场就显示全部；学期/学年按考试日期自动判断，不依赖手填学期。
- 横轴使用“短学期 + 考试名”的唯一名，例如 `26秋·第一次月考`；同一学期出现同名考试时会自动加序号。
- 班级趋势：选择全部科目时分成“各科平均分趋势”和“总分平均分趋势”两张图；选择某一单科时只显示该科。
- 学生趋势：选任一学生后，第一张图显示勾选的各科成绩，第二张图固定显示总分；两张图都可在折线图/柱状图之间切换。缺考科目留空，不会当成 0 分。
- 本版不提供下一场分数预测。

### 8. 知识点掌握分析（v1.5.2 新增）

- 选择一份**已经逐题批改**的作业：系统按题目的知识点标签统计。
- **全班统计**：每个知识点的满分、实得分、得分率和掌握情况（优秀>85% / 良好60–85% / 一般40–60% / 薄弱<40%），以及参与人数；下方自动列出最薄弱的 3 个知识点和改进建议。
- **学生掌握**：选一个学生，逐知识点看他的得分，绿底=掌握好、黄底=一般、红底=薄弱。
- 说明：考试只有科目总分、和题目没有关联，所以知识点分析走“作业逐题批改”，不能直接选考试；一道题打了多个知识点时，这道题的分分别计入每个知识点。没有逐题批改数据时会提示先去打分。

### 5. 学生画像

选学生后看到：信息卡、各科平均得分率**雷达图**（按得分率画，满分不同也可比）、自动标签（成绩层次、进步趋势、强项/弱项）、历次成绩表，以及「🤖 生成学情分析」个性化点评。

### 6. 教学反思（v1.0 新增）

- 选「单次考试」或「一段时间（起止两场考试）」，选班级。
- 点生成，AI 会结合**考试成绩指标 + 这段时间的作业错题**（错误类型、高频错题 TOP5）写出四段：**成功之处 / 不足之处 / 学生反馈 / 改进措施**。
- 生成后每段都能在线修改，点「💾 保存留档」，以后在“历史反思”里查看、导出 Word 或删除。
- 没有逐题批改数据时，会主要依据成绩分析，并在文中说明缺少作业数据。

### 7. 期末评语（v1.0 新增）

- 选班级、学期、风格（**鼓励 / 中肯 / 严格**）。
- 点「🤖 批量生成全部评语」：系统按学生**逐人调用**模型，有进度条和成功/失败计数，单条失败不影响其他人，失败的可点该行「🔄 重试生成」。
- 每条评语 100–150 字，可直接在框里改；可单条保存，也可「💾 一键保存全部」。
- 评语按「学生 + 学期」留档，同一学生同一学期重新生成会覆盖更新，不产生重复。
- 「📚 常用评语模板库」可增删自己的常用评语（供参考）。
- 点「⬇️ 导出本学期评语 Word」得到按班级、名单排版的评语集。

> 评语和反思只依据库里真实数据生成，不会编造获奖、职务等信息。
---

## 五、📚 备课

顶部 5 个标签页。

### 1. 资料管理

- 来源类型支持 PDF、Word、直接粘贴文本、网页链接。系统提取纯文本后按「第X章 / 第X节」自动切章节并预览。
- 上传文字版 PDF 时直接提取文字，不启动 OCR。
- 上传扫描件 PDF 后会转入后台本地 OCR；切换到其他页面不会中断。备课页的“后台 OCR 任务”会显示“正在识别第 X / N 页...”，返回资料页也能继续查看进度。
- OCR 完成后资料会自动出现在列表中，并把全文保存到 `data/uploads/text/`；确认内容无误后再点“向量化”。若识别失败，可按提示重新上传，或改用文字版 PDF、Word、粘贴文本。
- 使用网页链接时，选择「网页链接」，输入 `http://` 或 `https://` 开头的公开文章地址，点「提取并预览章节」。保存的是抓取当天的正文快照，开头会记录标题、来源 URL 和抓取日期。
- 同一网址已经保存过时，系统会直接提示已有资料，不会重复抓取或新建。
- 网页抓取只支持无需登录、正文直接在 HTML 里的公开页面；不支持整站爬取、批量链接、登录后内容、强 JavaScript 渲染页面，也不会下载图片、视频或附件。抓不到时可换链接，或直接复制网页正文后用“粘贴文本”导入。
- 提取的文本存在 `data/uploads/text/`，可随时查看。
- 点「向量化」把资料建索引。检索分三级：云端 embedding → 本地向量模型 → 纯关键词重叠，**没配 embedding 也不影响备课**，只是检索精度不同。

### 2. AI 备课

- 填课题、年级、课时、风格（传统 / 互动 / 探究），选择资料后勾选要使用的章节和教案模板。
- 生成结构固定的教案：三维目标、重点、难点、教学过程 5 环节（导入/新授/巩固/小结/作业，带分钟数）、板书设计、反思预设。
- 每个模块一个独立编辑框，可逐块修改；点保存留档，之后可导出排版清晰的 **Word**。

### 3. AI 出题

- 选知识点、题型、难度、题量，一次生成 5 道题，每题含题干、题型、难度、知识点、**答案**、分步解析、易错点。
- **没有答案的题一律不入库**（会提示拒收数）；模型给出可计算的算式时用 SymPy 自动验算，显示“通过/不一致/未验算”徽章。
- 生成题默认进「待审核」。

### 4. 题库管理

- 按题型、难度、知识点、审核状态、来源筛选，或按关键词搜索；题目列表默认最新创建的排在最前。
- 可审核（待审核→已审核）、编辑、删除（二次确认）。
- 支持从 **Word / Excel / 直接粘贴** 外部导入题目，缺答案的标红且默认待审核。
- 勾选题目后导出两种 Word：「仅题目」学生卷（含班级姓名得分行）、「题目+答案解析」教师卷；按勾选顺序重新编号。

### 5. PPT 生成

- 基于已保存的教案，离线生成 .pptx（不额外调用模型）：白底、深蓝标题，固定结构封面→目标→导入→新授→例题→练习→小结→作业。
- 每页要点不超过 5 条、每条截断到 20 字，可用 WPS / PowerPoint 打开二次编辑。

---

## 六、📝 作业

顶部 4 个标签页。

### 1. 作业管理

- 列表上方可按作业名称关键词、作业类型（预习/课中/课后/复习/试卷）和班级筛选；这三个筛选只作用于普通作业列表，模板仍只按学科显示。
- 点「新建作业/试卷」弹窗：5 种类型（📖预习 / 📝课中 / 📚课后 / 🔄复习 / 📄试卷），总分、用时等默认值随类型自动变，可从已有模板复制题目。
- 进入作业后，题目有 4 个来源：从**已审核题库勾选**、**AI 即时出题**、**外部导入**、**手动加题**。
- 可上下移动题目顺序、设置每题分值，总分实时汇总。
- 选「试卷」类型可点 **AI 自动组卷**：按题型题量和难度配比，从已审核题库确定性抽题，不足的部分调 AI 补齐，并提示知识点覆盖缺口；之后仍可增删调序改分。
- 预览学生卷 / 教师卷，导出两种 Word；可把作业「存为模板」复用；删除有二次确认。
- 列表里可勾选多份作业后“批量删除所选”（二次确认，模板不参与）。

### 2. 成绩录入

- **Excel 导入总分**：一列姓名 + 一个总分列，先预览（缺姓名/缺分标红），确认后写库，重复导入更新。
- **手动录入**：在表格里直接改总分，班内并列排名自动算。
- **逐题批改**：一行一个学生、一列一道题，录「对/错、得分、错误类型」（概念/计算/审题/书写/其他）。这是题目正确率和错题本的数据来源。

### 3. 作业分析

- 提交率、均分、最高/最低、分数段柱状图、班内排名表。
- 与上一份同班作业的进退步红绿标记。
- 每题正确率条形图、高频错题 TOP5（全班错误率 ≥40% 自动标“典型错题”）。
- 「🤖 AI 总结」给出整体→主要问题→教学建议（需配主模型，未配置会明确提示）。
- 可导出 Word 分析报告。

### 4. 错题本

- 逐题批改判错的题自动收集，无需另建错题表。
- 顶部先选学科，再按学生、作业、知识点、错误类型筛选；典型错题置顶。
- 详情含原题、该生作答得分、正确答案、解析（数学公式正常渲染），可导出 Word。
- 可导出按学生或按知识点分类的 **PDF**，PDF 中每题展示题干、答案、解析和错误次数。
- 每条错题可“➕ 加入下次作业”：题目进页面顶部题篮（去重），之后点“新建作业/试卷”，只要新作业同学科就会自动把题篮里的题带进去；学科不一致会提示且保留题篮。
- 点“🔓 打开这次作业”可直接切到作业管理并打开那次作业。
- **错题重做卷**：勾选多条错题后点“用勾选错题生成重做卷”，先检查预览表里的难度和分值，确认后生成普通课后作业；同题被多名学生做错只保留一次。修改难度会复制成新题，不改原题。
- 题库管理里多选待审核题可“✅ 批量审核通过”。

---

## 七、📅 教学日历与课程安排

### 1. 查看月历
- 左侧进入「📅 教学日历」，可上一月、下一月切换。
- 每格显示日期和当天的考试、作业、教案；直接点击日期格，下方会自动显示当天安排；今天前面有蓝色圆点并有浅蓝底色。
- 日期不会再显示成 `**15**`。

### 2. 查看某天与本周概览
- 在“查看某天”选择日期，可查看当天完整安排，并跳转到作业、学情或备课页。
- “本周概览”列出本周考试、作业、教案，以及未来 7 天考试和未录完成绩的作业。

### 3. 课程安排表
- 先选择班级，再在 7 天 × 8 节表格中填写学科；清空某个单元格并保存，会删除这节课。
- 课程表按班级分别保存，保存一个班不会影响其他班级。
- 可下载 `课程安排表模板.xlsx`，按“星期、节次、科目、班级”填写后导入。
- 星期支持 `1`、`周一`、`星期一`；节次支持 `1`、`第1节`；Excel 中的班级必须已经存在。

### 4. 首页入口
- 首页快捷入口下方显示“本周日历预览”和“本周课程表”。
- 点击“📅 打开完整教学日历”可进入完整月历和课程表管理。

---

## 八、⚙️ 设置与数据管理

### 0. 各功能独立选择学科

v1.3.0 起没有“全局当前学科”。语文、数学、英语、物理、化学、生物、政治、历史、地理 9 个学科的选择器分散在具体功能里，各自记忆、互不影响：

- 备课页：资料管理、AI 备课、AI 出题、题库管理、PPT 生成 5 个 Tab 各有一个“学科”选择器。
- 作业页：作业管理、新建作业/试卷弹窗、错题本各有一个“学科”选择器。
  - 作业列表右上角的“学科”只负责筛选已有作业和模板。
  - 新建作业弹窗里的“学科”只决定这一份新作业的归属，不会改变列表筛选。
- 资料管理切到物理，不会改变题库管理或作业管理的学科；下次重启后，每个功能仍停留在上次选择的学科。
- 新保存的资料、教案、题目、作业会记录当时该功能选择的学科；题库 Word、错题本 Word 的标题和文件名带对应学科名。
- 已打开的作业始终使用这份作业自己的学科，即使作业列表后来切到别的学科，加题、组卷和改分也不会串科。

注意：

- **应用标题/图标始终固定为“📐 AI教学辅助”**，不随学科变化。
- 成绩录入和作业分析不提供学科筛选：作业下拉会显示全部普通作业，并用“[数学]”“[物理]”这样的标签标明学科。
- 历史成绩仍按 Excel 列名识别，可用考试分析的“分析视角”和趋势分析的“科目”选择器查看。

### 0.1 班级管理
- 学情 →「学生管理 → 🏫 班级管理」：班级名单是一个可编辑表格，可直接在“班级名”列改名或用最后一行新增，点“💾 保存班级修改”统一生效；“学生数”自动统计，不用填写。也可一次选择多名学生批量调班。
- 删除班级走表格最前“删除”勾选列 + “🗑️ 删除勾选班级”，需二次确认；为避免误删，保存修改和删除是两个独立操作。
- 重命名会同步学生和作业中的班级；删除有学生或作业的班级会被阻止，需先调班或重命名。
- 班级名单存在本地 `data/class_names.json`，不是独立数据表。
- 极早期没有学科标记的资料仍会在数学资料列表中显示；历史题目和作业默认按数学处理。

### 0.2 考试分析线
- 学情 →「单次考试分析」顶部的「⚙️ 考试分析线」可调整及格线和优秀线百分比，默认 60% 和 85%。
- 设置会保存到本地 `data/analysis_thresholds.json`，考试分析、考试 Word、学期报告和教学反思统一使用。
- 只允许填写 1–99 的数字，且优秀线必须高于及格线。

### 1. 备份（建议定期做）

设置 →「数据备份与恢复」→「📦 备份」：

- **生成并下载备份**：把整个 `data/`（数据库、上传资料、向量库、AI 非敏感配置）打成一个带时间戳的 zip，建议存到 U 盘或网盘。
- **在本机留一份**：备份保存在 `data/backups/`。
- 已有的备份 zip 和临时锁文件不会重复打进新包；API Key 不在备份里。

### 2. 恢复（换电脑 / 数据出问题时）

设置 →「数据备份与恢复」→「♻️ 恢复」：

1. 上传之前生成的备份 zip。
2. 点确认恢复。系统会先校验压缩包，再把当前 `data/` 整体改名成 `data.bak-时间戳` 留底，然后解包备份。
3. **恢复成功后务必关闭并重新启动 Streamlit**（重启 `streamlit run app.py`），再刷新页面。
4. 换电脑时，恢复后到设置页重新填一次 API Key。

### 3. 清空业务数据（危险操作）

设置 →「数据备份与恢复」→「🗑️ 清空业务数据」：

- 删除全部学生、成绩、考试、作业、题目、教案、资料、反思、评语等 **13 张业务表**的数据。
- **保留** AI 配置、API Key、已有备份 zip；数据库表结构不动。
- 需输入“确认清空”并二次确认，防止误触。清空前强烈建议先备份。

### 4. 数据都存在哪

| 内容 | 位置 |
| --- | --- |
| 数据库 | `data/database.db` |
| 上传资料、提取文本 | `data/uploads/` |
| 导出文件 | `data/exports/` |
| 向量库 | `data/chroma/` |
| AI 非敏感配置 | `data/llm_config.json` |
| 各功能学科选择 | `data/feature_subjects.json` |
| 考试分析及格线/优秀线 | `data/analysis_thresholds.json` |
| 课程安排表 | `data/class_schedule.json` |
| 本地备份 | `data/backups/` |
| API Key | Windows 凭据管理器（服务名 math-ai-teacher） |

---

## 九、常见问题

**Q：运行后满屏 `missing ScriptRunContext` 警告？**
A：你直接运行了 app.py。必须用 `streamlit run app.py`（PyCharm 里配成“模块名 streamlit + 参数 run app.py”）。

**Q：AI 按钮点了提示“还没配置 API Key”？**
A：到「⚙️ 设置」填主模型 Key、Base URL、模型名，先点测试连接成功再用。

**Q：提示“找不到这个模型 / 401 / 超时”？**
A：分别核对模型名、API Key、网络与 Base URL；程序已自动重试 2 次，仍失败可换网络或稍后再试。

**Q：网页提示抓取失败怎么办？**
A：先确认链接是 `http://` 或 `https://` 开头、页面无需登录；需要登录或主要靠 JavaScript 加载的页面暂不支持。可以换一篇文章链接，或在浏览器里复制正文，用“粘贴文本”方式导入。

**Q：没配向量模型能备课吗？**
A：能。会自动降级到本地向量、再降级到关键词检索，只是语义检索精度下降。

**Q：PDF 成绩表为什么识别不到？**
A：成绩表只支持带规范线框的表格 PDF，资料类扫描件 PDF 才会自动 OCR；成绩扫描件、无线框排版仍建议先整理到 Excel，按「姓名 + 各科分数」导入。

**Q：导入成绩后名次、及格率不对？**
A：检查这场考试的「各科满分」是否设对（考试管理里可改）；及格线和优秀线默认按满分 60%、85%，也可在「学情 → 考试分析 → 考试分析线」调整。

**Q：成绩 / 评语改错了能撤回吗？**
A：单条可直接在页面修正并重新保存（评语按学生+学期覆盖更新）。整库层面建议改动前先做一次备份，必要时用恢复功能回到备份时的状态。

**Q：示例数据在哪？**
A：命令行执行 `python scripts/make_demo_data.py`，默认只在 `data/uploads/demo/` 生成 3 个班、90 名学生、10 场考试的虚构 Excel；加 `--wipe` 会先备份、清空业务表，再把 demo 写入数据库。

